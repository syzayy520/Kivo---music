#pragma once

#include <stdexcept>

#define OUTPUT_TRUTH_ASSERT(condition) \
    do { \
        if (!(condition)) { \
            throw std::runtime_error("Assertion failed: " #condition); \
        } \
    } while (false)
